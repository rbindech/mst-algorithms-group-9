# mst-algorithms-group-9
1. Implement Prim’s algorithm to find MST 2. Implement Kruskal’s algorithm to find MST 3. Independently find another algorithm than any explained on this slide, then implement it to find MST 4. Simulate if any node/edge failed (cannot be traversed), report how your algorithm adapt
